{
  global:
    # FreeBSD needs these two:
    # Android fails to link cause of these two...
    #__progname;
    #environ;
    # Allow users to load the server as a shared library with dlopen() and call main() directly
    main;

  local:
    *;
};
